// const userList = getUserList();

// function getUserList(){

export const getUserList = () => {
    return [
        {
            id: 1,
            isPublic: true,
            nama: 'user1',
            status: 'active'
        },
        {
            id: 2,
            isPublic: true,
            nama: 'user2',
            status: 'active'
        }
    ]
}